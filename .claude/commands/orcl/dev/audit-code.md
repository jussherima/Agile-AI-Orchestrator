Audite la qualité, la sécurité et la performance du code, étape par étape.

Lis `_orcl/workflows/audit-code/workflow.md` pour comprendre le workflow complet.

<execution>
Pour chaque étape du workflow :
1. Lis UNIQUEMENT le fichier step en cours (pas les suivants)
2. Exécute les instructions du step
3. Quand le step est terminé, lis le step suivant
4. Ne saute jamais une étape

Commence par lire `_orcl/workflows/audit-code/steps/step-01-scope.md`.
</execution>

<constraints>
<do-not>Ne jamais charger tous les steps d'un coup.</do-not>
<do-not>Ne jamais ignorer les vulnérabilités OWASP.</do-not>
<do-not>Ne jamais valider du code avec des secrets hardcodés.</do-not>
</constraints>

Si besoin de référence → `brain/dev/ORCL_SECURITY_PERFORMANCE.md`.

Scope de l'audit : $ARGUMENTS
