Recherche les best practices, packages et conventions d'un framework, étape par étape.

Lis `_orcl/workflows/recherche-stack/workflow.md` pour comprendre le workflow complet.

<execution>
Pour chaque étape du workflow :
1. Lis UNIQUEMENT le fichier step en cours (pas les suivants)
2. Exécute les instructions du step
3. Quand le step est terminé, lis le step suivant
4. Ne saute jamais une étape

Commence par lire `_orcl/workflows/recherche-stack/steps/step-01-identifier.md`.
</execution>

<constraints>
<do-not>Ne jamais charger tous les steps d'un coup.</do-not>
<do-not>Ne jamais recommander un package non maintenu ou obsolète.</do-not>
<do-not>Ne jamais ignorer les conventions officielles du framework.</do-not>
</constraints>

Si un document de référence existe dans `brain/stacks/` → le charger comme base.

Framework à rechercher : $ARGUMENTS
